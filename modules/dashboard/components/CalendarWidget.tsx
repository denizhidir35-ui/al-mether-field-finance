import { CalendarDays, ChevronRight, Clock3 } from "lucide-react";
import type { CalendarEvent } from "../types";

export function CalendarWidget({ events }: { events: readonly CalendarEvent[] }) {
  return <article className="mether-surface rounded-[18px] p-3.5"><header className="flex items-center justify-between"><div className="flex items-center gap-2"><span className="grid h-8 w-8 place-items-center rounded-lg bg-cyan-500/10 text-cyan-300"><CalendarDays size={15} /></span><div><h2 className="text-[14px] font-bold text-white">Takvim</h2><p className="text-[9px] text-slate-500">Yaklaşan program</p></div></div><button className="text-slate-500 hover:text-blue-400" aria-label="Takvimi aç"><ChevronRight size={15} /></button></header><div className="mt-2 space-y-1.5">{events.slice(0, 2).map(event => <div key={event.id} className="flex items-center gap-2.5 rounded-xl border border-white/[0.045] bg-white/[0.014] px-2.5 py-1.5"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-lg bg-cyan-500/[0.07]"><div className="text-center"><div className="text-[12px] font-black leading-none text-cyan-200">{event.day}</div><div className="mt-0.5 text-[8px] font-bold text-cyan-400/70">{event.month}</div></div></div><div className="min-w-0 flex-1"><div className="truncate text-[11px] font-semibold text-slate-300">{event.title}</div><div className="mt-0.5 text-[9px] text-slate-500">{event.context}</div></div><span className="flex items-center gap-1 text-[9px] text-slate-400"><Clock3 size={9} />{event.time}</span></div>)}</div>{events.length > 2 ? <div className="mt-1.5 text-center text-[10px] font-semibold text-blue-400">+{events.length - 2} etkinlik daha</div> : null}</article>;
}
