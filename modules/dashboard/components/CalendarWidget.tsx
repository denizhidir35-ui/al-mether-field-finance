import { CalendarDays, ChevronRight, Clock3 } from "lucide-react";
import type { CalendarEvent } from "../types";

export function CalendarWidget({ events }: { events: readonly CalendarEvent[] }) {
  return <article className="mether-surface rounded-[18px] p-3.5"><header className="flex items-center justify-between"><div className="flex items-center gap-2"><span className="grid h-7 w-7 place-items-center rounded-lg bg-cyan-500/10 text-cyan-300"><CalendarDays size={14} /></span><div><h2 className="text-[12px] font-bold text-white">Takvim</h2><p className="text-[7px] text-slate-600">Yaklaşan program</p></div></div><button className="text-slate-600 hover:text-blue-400" aria-label="Takvimi aç"><ChevronRight size={14} /></button></header><div className="mt-2.5 space-y-1.5">{events.map(event => <div key={event.id} className="flex items-center gap-2.5 rounded-xl border border-white/[0.045] bg-white/[0.014] px-2.5 py-1.5"><div className="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-cyan-500/[0.07]"><div className="text-center"><div className="text-[10px] font-black leading-none text-cyan-200">{event.day}</div><div className="mt-0.5 text-[6px] font-bold text-cyan-400/70">{event.month}</div></div></div><div className="min-w-0 flex-1"><div className="truncate text-[9px] font-semibold text-slate-300">{event.title}</div><div className="mt-0.5 text-[7px] text-slate-600">{event.context}</div></div><span className="flex items-center gap-1 text-[7px] text-slate-500"><Clock3 size={8} />{event.time}</span></div>)}</div></article>;
}
